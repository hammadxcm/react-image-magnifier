/**
 * Position-related types for the magnifier
 */

export interface Position {
  x: number;
  y: number;
  mouseX: number;
  mouseY: number;
}

export interface ImageSize {
  width: number;
  height: number;
}

export interface Coordinates {
  x: number;
  y: number;
}
