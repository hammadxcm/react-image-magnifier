/**
 * Central type exports for the image magnifier library
 */

// Position types
export type { Position, ImageSize, Coordinates } from './position';

// Magnifier types
export type {
  MagnifierShape,
  MagnifierPosition,
  CursorStyle,
  MagnifierConfig,
  ZoomConfig,
  ImageConfig,
  BehaviorConfig,
  StyleConfig,
} from './magnifier';

// Theme types
export type { MagnifierTheme, GlobalSettings, ThemeName } from './theme';
